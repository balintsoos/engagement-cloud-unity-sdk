package com.sap.ec.mobileengage.embeddedmessaging.ui

internal actual fun initializeCustomElements() {
    // Embedded-messaging UI is not part of Phase 1 macOS parity — no custom elements to register.
}
